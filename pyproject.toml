[project]
name = "netclip"
version = "0.1.0"
description = "NetClip - LAN-Zwischenablage"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
extend-exclude = [".worktrees"]
